#ifndef BINARIO_H
#define BINARIO_H
#include "autores.h"
#include "documento.h"

void salvarPilha(Pilha *p, char nomeArquivo[50]);
void carregarPilha(Pilha *p, char nomeArquivo[50]);

#endif // BINARIO_H